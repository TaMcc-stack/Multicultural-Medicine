# 生产环境变量模板
# ============================================================
# 用法：复制为 .env.production 后再改。
#
#     cp .env.production.example .env.production
#
# 没有 .env.production 时，前端 request.ts 的 baseURL 会回退成同源 '/api'。
# ============================================================

# ⚠️ 只能填【一个】地址。
#
# 本应用有【两个】后端：
#     Spring Boot  :8080   登录 / 会话 / 知识库 / 动态 / 收藏
#     FastAPI      :8000   问题理解 / 证据检索 / 回答生成
#
# 而 axios 只有一条 baseURL。所以生产环境必须先有一个反向代理，
# 把 /api 按前缀拆到这两个后端（拆分规则见 README-DEPLOY.md 第二节），
# 这里填那个反代（或网关）的地址。
#
# 前端与反代【同域】时：整行留空即可（回退到同源 '/api'）——推荐做法，
# 好处是全部请求变成同源，两个后端的 CORS 一行都不用改。
#
# 前端与反代【跨域】时：填反代的完整地址（不要带结尾斜杠）。
# VITE_API_BASE=https://api.example.com
