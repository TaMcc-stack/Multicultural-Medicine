# 多民族特色医学智能体 · 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 多民族特色医学智能体 |
| 产品形态 | 前端 HTML 原型 + 可选 FastAPI 语义理解后端（Qwen / 阿里云百炼 DashScope） |
| 领域 | 多民族精准用药 |
| 文档版本 | v1.1 |
| 更新日期 | 2026-08-31 |
| 状态 | 概念验证（Prototype） |

---

## 1. 产品概述

### 1.1 背景

我国是一个多民族国家，不同民族人群在疾病谱、患病率、药物代谢相关基因多态性等方面存在差异。临床上往往缺少针对「某个民族 × 某类疾病」的、可被追溯依据的精准用药参考。

### 1.2 产品定位

一个面向多民族精准用药场景的**智能问答智能体**：用户用自然语言提问，智能体先理解问题，再从知识库中定位证据，最后给出**有依据、可溯源**的回答。

### 1.3 核心价值主张

> **回答有依据，并且可以追溯到对应论文和证据片段。**

即：每一次回答都不是凭空生成，而是明确指向某一份资料中的某一个表格或某一段原文，用户可以直接跳到知识库中查看原文。

### 1.4 三页面信息架构

| 页面 | 职责 | 面向对象 |
| --- | --- | --- |
| `index.html` 首页 | 提问、验证、发送、展示最终回答 | 普通用户 |
| `kb.html` 知识库 | 资料的入库、分类、全文存储与查阅 | 资料维护者 |
| `backend.html` 后台 | 展示「问题理解 → 知识库提取 → 加工」全流程 | 演示者 / 开发者 |

---

## 2. 目标用户与使用场景

- **临床医生 / 科研人员**：查询「某民族人群某疾病的患病率、影响因素、用药依据」等。
- **产品演示者**：向客户/评审展示「问题如何被理解、证据如何被定位、回答如何被溯源」的完整链路。

典型场景：用户在首页输入「白族糖尿病患病率如何」，验证通过并发送后，系统返回「白族糖尿病的患病率为 16.0%」，并附上剪贴自《数据资料》第 3 页的患病率表格作为证据，点击来源可跳转到知识库对应资料。

---

## 3. 核心业务流程

```
用户提问
   │
   ▼
【验证】（结构化拆解：民族 + 疾病 + 查询意图）
   │
   ├─ 三者齐全 ──────────────► 验证通过，按钮变为「可以发送」
   │
   ├─ 缺「查询意图」─────────► 验证失败 · 问题模糊
   │
   └─ 缺「民族」或「疾病」───► 验证失败 · 信息不足
   │
   ▼
【发送】→ 后台开始处理（模拟约 1.2 秒）
   │
   ▼
【首页直接展示回答】
   ├─ 用户问题
   ├─ 证据材料（表格 / 原文片段，剪贴自某份资料）
   └─ 结论（可溯源到资料 + 页码）
   │
   └─ 点击来源标题 → 跳转知识库定位并高亮对应资料
```

---

## 4. 功能需求（分页面）

### 4.1 首页（index.html）

#### 4.1.1 提问区
- 顶部品牌栏：左侧产品名，右侧「知识库」「后台」入口。
- 中央问题输入框（placeholder：`例如：白族糖尿病患病率如何`）。
- 右侧椭圆形「验证」按钮。

#### 4.1.2 验证按钮状态机
| 状态 | 显示 | 触发 |
| --- | --- | --- |
| 初始 | 「验证」 | 页面加载 / 输入内容变更 |
| 通过 | 「可以发送」（绿色） | 验证通过 |
| 处理中 | 「处理中…」（琥珀色，禁用） | 点击「可以发送」后 |
| 完成 | 恢复「验证」 | 回答生成完成 |

#### 4.1.3 提示（Toast）
- 验证通过：`验证完毕，问题很清楚，可以发送`（绿色，3 秒）。
- 问题模糊：`验证失败：问题模糊，查询意图不明确…`（红色）。
- 信息不足：`验证失败：信息不足，请补充「民族 + 疾病」…`（红色）。

#### 4.1.4 回答卡片（发送后展示）
回答卡片包含：
1. 标题「回答」+ 徽章「有依据 · 可溯源」。
2. 「问：」用户问题。
3. 「证据材料 · 剪贴自《资料名》」——表格或原文片段。
4. 结论：`根据《X》第 N 页的内容，可得出：白族 糖尿病 的患病率为 16.0%。`
5. 「查看后台完整处理过程 →」链接（跳转后台）。
6. 来源《资料名》为可点击链接，点击跳转知识库对应资料并高亮。

#### 4.1.5 交互细节
- 输入内容变更时：按钮复位「验证」、自动隐藏旧回答。
- 回答生成后自动滚动到回答卡片。

### 4.2 知识库（kb.html）

#### 4.2.1 书架展示
- 资料按「类别」分书架展示（默认分类：`民族疾病类`）。
- 每个书架显示类别名、资料数量。
- 每份资料以卡片形式陈列，卡片含：名称《标题》、删除按钮、内容摘要（可展开全文）。

#### 4.2.2 添加资料
- 右上角「添加资料」按钮打开弹窗，或直接**拖拽文件**到书架。
- 支持拖拽 / 点击选择本地文件（`.docx` 等）。
- 自动识别**文件名称**填入「资料名称」。
- 类别默认填入 `民族疾病类`。
- **全文提取**：`.docx` 通过 mammoth.js 提取全文；不支持的类型/解析失败则留空。
- 表单字段精简为：`资料名称`、`类别`、`文档内容（全文）`（不做民族/疾病/主题单字段拆分，因一份资料含多民族多疾病）。

#### 4.2.3 删除资料
- 每张卡片右上角「移除」按钮，**两段式确认**：点击变「确认删除」（红色），再点确认删除；3 秒未确认自动恢复。

#### 4.2.4 内容展示
- 卡片内容默认收起（约 6 行截断），点击「展开全文 / 收起」切换。

#### 4.2.5 深链接定位
- 支持 URL hash（`kb.html#REF-0001`）定位到对应资料卡片，自动滚动并高亮闪烁（约 2.6 秒）。

### 4.3 后台（backend.html）

后台用于可视化「问题如何被理解、证据如何被定位、回答如何被加工」的完整链路。三个栏目**始终展示**，无问题时内容均为「无」。

#### 4.3.1 问题理解
- 待分析问题栏：显示问题文本 + 状态徽章。
- 结构化拆解：`民族 / 疾病 / 查询意图` 三项。
- 验证结论条（三种颜色）：
  - 灰色：`未收到问题`。
  - 绿色：`验证通过 · 问题清楚，可以进行下一步发送`。
  - 红色：`验证失败 · 问题模糊 / 信息不足`。

#### 4.3.2 知识库提取（后台小智能体）
- 流程：识别结构化内容 → 前往【民族疾病类】筛选匹配「民族 + 疾病」的资料 → 找到并打包。
- 展示打包结果：《资料名》+ 全文字数 + 证据（表格）。

#### 4.3.3 加工栏（大模型）
- 角色说明：把处理者称作「大模型」，负责先理解问题、再到资料中找对应内容并加工成回答。
- **两份材料**：材料① 问题；材料② 知识库资料。
- 加工三步：
  1. 大模型先**理解问题**，识别结构化内容（民族/疾病/查询意图）。
  2. 打开《资料》，对照「民族 · 疾病 · 查询意图」找这一部分内容，找到的是「一个表格（第 N 页）」或「原文里的一段话」。
  3. 把这一部分内容**单独剪贴**出来，放在「问题处理框」的**用户问题下面**，当作证据材料。
- 问题处理框：用户问题 → 证据材料（剪贴自《资料》）→ 结论。

#### 4.3.4 展开逻辑
- 无问题/未通过：仅展开【问题理解】。
- 验证通过：三个栏目**全部自动展开**，直接看到完整流水线。

---

## 5. 核心业务规则

### 5.1 问题验证 / 结构化拆解

拆解目标三要素：`民族`、`疾病`、`查询意图`。

- **首选**：调用语义理解 API（见 §5.5），由 Qwen 做结构化拆解并给出置信度 `confidence`（0~1）。
- **降级**：后端未启动 / 未配置 key / 调用失败时，前端回退到内置关键词规则匹配（`firstMatch` 首个命中）。

置信度三档判定（置信度 = 大模型对结构化理解结果的确定程度，0~1）：
| 置信度 | status | 结论 |
| --- | --- | --- |
| ≥ 0.8 | `pass` | 验证通过 → 按钮变为「可以发送」 |
| 0.6 ~ 0.8 | `clarify` | 建议澄清（如查询意图模糊） |
| < 0.6 | `clarify` | 必须澄清 |

> 民族/疾病完全缺失等无法理解的情形，由前端判定为 `fail`（提示补充民族+疾病）。
> 置信度代表「我有多确定我正确理解了用户想问什么」，不是语义偏离度。

关键词规则（降级方案）：
| 条件 | 结论 |
| --- | --- |
| 民族 + 疾病 + 查询意图 三者齐全 | 清楚 → 验证通过 |
| 有民族 + 疾病，但缺查询意图 | 建议澄清 |
| 缺民族 或 缺疾病 | 无法理解 |

关键词列表：
- **民族**：白族、傣族、哈尼族、汉族、苗族、彝族、维吾尔族、藏族、回族、蒙古族、朝鲜族、景颇族、傈僳族、佤族、普米族、布朗族、纳西族、土家族、满族、布依族、壮族、畲族、哈萨克族。
- **疾病**：糖尿病、高血压、代谢综合征、CKM、心脏瓣膜病、非酒精性脂肪性肝病、NAFLD、肥胖、乙型肝炎、乙肝、慢性肾病。
- **查询意图**：患病率、发病率、检出率、影响因素、危险因素、研究发现、基因、遗传、多态性、用药、药物、不良反应、安全性、剂量。

示例：
- `白族糖尿病患病率如何` → 白族 / 糖尿病 / 患病率 → **通过**（API 置信度约 0.93）。
- `白族患糖尿病的人多不多啊` → 白族 / 糖尿病 / 患病率（口语归一化）→ **通过**。
- `白族糖尿病情况如何` → 白族 / 糖尿病 / 无意图 → **建议澄清**。

### 5.2 知识库匹配规则

在类别为 `民族疾病类` 的资料中，检索标题 + 民族 + 疾病 + 全文内容，同时包含「民族」和「疾病」关键词的资料视为命中；取首个命中的资料。

### 5.3 证据生成规则

| 资料情况 | 证据材料 |
| --- | --- |
| 资料带有结构化表格（`table`） | 剪贴表格（命中民族行高亮） |
| 资料仅有文字 | 剪贴原文片段（最长 220 字，超出省略号截断） |

### 5.4 回答生成规则

```
根据《资料名》里第 N 页的内容：
（证据材料见剪贴内容）
可得出：{民族} {疾病} 的患病率为 {数值}%。
```
- 数值从证据表格中按民族行查找；无表格/无数值时显示「（见证据材料）」。

### 5.5 语义理解 API（后端）

可选 FastAPI 后端（`backend/`），把「问题理解」从关键词匹配升级为真实大模型语义理解。

- **技术栈**：Python + FastAPI，调用 Qwen（阿里云百炼 DashScope，OpenAI 兼容接口）。
- **接口**：`POST /api/understand`，请求 `{"question": "白族患糖尿病的人多不多啊"}`。
- **响应**：

```json
{
  "ethnicity": "白族",
  "disease": "糖尿病",
  "intent": "患病率",
  "confidence": 0.93,
  "status": "pass"
}
```

- **配置**：`DASHSCOPE_API_KEY` 经环境变量（`.env`）注入，不硬编码；`QWEN_MODEL` 默认 `qwen-plus`。
- **错误处理**：未配置 key 返回 503、Qwen 调用失败返回 502；前端据此降级为关键词匹配。

---

## 6. 数据模型

### 6.1 问题对象（localStorage）

```json
{
  "text": "白族糖尿病患病率如何",
  "ethnic": "白族",
  "disease": "糖尿病",
  "intent": "患病率",
  "status": "clear | vague | incomplete",
  "confidence": 0.93,
  "ts": 1725100000000
}
```

> `confidence` 为可选字段：仅当语义理解 API 命中时写入；关键词降级时缺省。

### 6.2 资料对象（localStorage）

```json
{
  "id": "REF-0001",
  "title": "数据资料",
  "category": "民族疾病类",
  "content": "……（全文，可长达数千字）……",
  "table": {
    "page": 3,
    "caption": "表 · 各民族糖尿病标化患病率（部分）",
    "header": ["民族", "糖尿病标化患病率（%）"],
    "rows": [["白族", "16.0"], ["傣族", "7.6"], ["哈尼族", "5.0"]],
    "note": "χ²=105.856，P＜0.001"
  }
}
```
> `table` 为可选字段：有则走表格证据，无则走原文片段证据。

### 6.3 状态存储键

| 键 | 用途 |
| --- | --- |
| `mmx_question_v2` | 首页验证后的问题对象 |
| `mmx_kb_lit_v2` | 知识库资料列表 |

---

## 7. 设计规范

### 7.1 色彩系统（暖色医学风）

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#FBF6EF` | 页面背景 |
| `--surface` | `#FFFFFF` | 卡片/表面 |
| `--ink` | `#2B241D` | 主文字 |
| `--ink-2` | `#6B5D4E` | 次级文字 |
| `--ink-3` | `#9A8B78` | 辅助文字 |
| `--clay` | `#B85A36` | 主强调色（陶土红） |
| `--sage` | `#3F6A58` | 成功/确认（鼠尾草绿） |
| `--amber` | `#B77E2A` | 提示/处理中（琥珀） |
| `--danger` | `#B4492F` | 错误/删除 |

### 7.2 字体
- 标题/品牌：`Noto Serif SC`（衬线，宋体感）。
- 正文：`Noto Sans SC`（无衬线）。

### 7.3 关键视觉元素
- 首页「验证/发送」为**椭圆形按钮**。
- 后台三大栏目为可折叠卡片，`【问题理解】【知识库提取】【加工栏】`。
- 证据表格：命中民族行以琥珀色高亮。

---

## 8. 非功能需求

- **实现形态**：前端 HTML + 原生 JavaScript + localStorage；可选 FastAPI 语义理解后端（Qwen/DashScope），后端缺席时前端自动降级为关键词匹配，不影响原型运行。
- **演示数据**：预置《数据资料》作为唯一种子资料；所有医学数据为真实来源（见 §9）。
- **文件解析**：`.docx` 依赖 mammoth.js（CDN：`jsdelivr/npm/mammoth@1.6.0`）提取全文；PDF 暂不支持解析。
- **兼容性**：现代浏览器（Chrome / Edge 等），本地双击打开或简单静态服务器均可运行。

---

## 9. 参考资料来源

| 资料 | 用途 | 关键数据 |
| --- | --- | --- |
| `数据资料(2).docx` | 种子资料（全文入库） | 白族糖尿病标化患病率 16.0%、傣族 7.6%、哈尼族 5.0%（χ²=105.856，P＜0.001） |
| `中国多民族人群精准用药研究价值与策略专家共识(2).pdf` | 待入库的纯文字资料（用于演示「原文片段」证据形态） | — |

---

## 10. 已知限制与后续规划

### 10.1 已知限制
- 结构化拆解：已提供语义理解 API（Qwen）作为首选，但**未启动后端时**仍降级为关键词规则匹配，仅覆盖预置词表。
- 回答与证据均为**前端模拟**，未接入真实检索与生成模型（RAG）。
- 验证与「后台处理」之间为固定延时模拟，非真实异步计算。
- 证据「原文片段」为全文截断，未做真实的相关段落定位。

### 10.2 后续规划（建议）
1. （已具备）语义理解 API 已接入 Qwen 完成意图识别与置信度评分；下一步可增加「澄清追问」交互闭环。
2. 接入真实文档检索 / RAG，实现段落级证据定位与引用页码自动标注。
3. PDF 全文解析与 OCR 表格识别。
4. 用户问答历史与多轮对话。
5. 知识库批量导入、标签体系与检索排序。
6. 后端服务化与数据持久化（替换 localStorage）。

---

*本文档描述产品当前原型（Prototype）的目标形态，随迭代持续更新。*
